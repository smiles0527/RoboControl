<UserControl x:Class="ControlWorkbench.App.Views.VexRobotConfigView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d" 
             d:DesignHeight="700" d:DesignWidth="1200"
             Background="#1E1E1E">
    
    <UserControl.Resources>
        <Style TargetType="TextBlock" x:Key="HeaderStyle">
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontSize" Value="16"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Margin" Value="0,0,0,10"/>
        </Style>
        
        <Style TargetType="TextBlock" x:Key="LabelStyle">
            <Setter Property="Foreground" Value="#CCCCCC"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="Margin" Value="0,8,0,4"/>
        </Style>
        
        <Style TargetType="TextBox" x:Key="InputStyle">
            <Setter Property="Background" Value="#3E3E42"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderBrush" Value="#555555"/>
            <Setter Property="Padding" Value="8,6"/>
            <Setter Property="FontSize" Value="13"/>
        </Style>
        
        <Style TargetType="ComboBox" x:Key="ComboStyle">
            <Setter Property="Background" Value="#3E3E42"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Padding" Value="8,6"/>
            <Setter Property="FontSize" Value="13"/>
        </Style>
        
        <Style TargetType="Button" x:Key="PrimaryButton">
            <Setter Property="Background" Value="#0078D4"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Padding" Value="20,10"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
        </Style>
        
        <Style TargetType="Button" x:Key="SecondaryButton">
            <Setter Property="Background" Value="#3E3E42"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Padding" Value="15,8"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
        </Style>
    </UserControl.Resources>
    
    <Grid Margin="20">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="350"/>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="400"/>
        </Grid.ColumnDefinitions>
        
        <!-- Left Column - Team & Drivetrain -->
        <ScrollViewer Grid.Column="0" VerticalScrollBarVisibility="Auto">
            <StackPanel Margin="0,0,20,0">
                <!-- Team Info -->
                <Border Background="#2D2D30" Padding="20" CornerRadius="8" Margin="0,0,0,20">
                    <StackPanel>
                        <TextBlock Text="?? Team Information" Style="{StaticResource HeaderStyle}"/>
                        
                        <TextBlock Text="Team Number" Style="{StaticResource LabelStyle}"/>
                        <TextBox x:Name="TeamNumber" Text="1234A" Style="{StaticResource InputStyle}"/>
                        
                        <TextBlock Text="Robot Name" Style="{StaticResource LabelStyle}"/>
                        <TextBox x:Name="RobotName" Text="Competition Bot" Style="{StaticResource InputStyle}"/>
                        
                        <TextBlock Text="Code Library" Style="{StaticResource LabelStyle}"/>
                        <ComboBox x:Name="LibraryChoice" Style="{StaticResource ComboStyle}" SelectedIndex="0">
                            <ComboBoxItem Content="LemLib (Recommended)"/>
                            <ComboBoxItem Content="EZ-Template"/>
                            <ComboBoxItem Content="PROS Only (Custom)"/>
                            <ComboBoxItem Content="VEXcode Pro"/>
                        </ComboBox>
                    </StackPanel>
                </Border>
                
                <!-- Drivetrain Config -->
                <Border Background="#2D2D30" Padding="20" CornerRadius="8" Margin="0,0,0,20">
                    <StackPanel>
                        <TextBlock Text="?? Drivetrain" Style="{StaticResource HeaderStyle}"/>
                        
                        <TextBlock Text="Drive Type" Style="{StaticResource LabelStyle}"/>
                        <ComboBox x:Name="DriveType" Style="{StaticResource ComboStyle}" SelectedIndex="0">
                            <ComboBoxItem Content="Tank Drive (6 motor)"/>
                            <ComboBoxItem Content="Tank Drive (4 motor)"/>
                            <ComboBoxItem Content="H-Drive"/>
                            <ComboBoxItem Content="X-Drive"/>
                            <ComboBoxItem Content="Mecanum"/>
                        </ComboBox>
                        
                        <TextBlock Text="Motor Cartridge" Style="{StaticResource LabelStyle}"/>
                        <ComboBox x:Name="CartridgeType" Style="{StaticResource ComboStyle}" SelectedIndex="2">
                            <ComboBoxItem Content="?? Red (100 RPM) - High Torque"/>
                            <ComboBoxItem Content="?? Green (200 RPM) - Standard"/>
                            <ComboBoxItem Content="?? Blue (600 RPM) - High Speed"/>
                        </ComboBox>
                        
                        <Grid Margin="0,10,0,0">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                            </Grid.RowDefinitions>
                            
                            <StackPanel Grid.Column="0" Grid.Row="0" Margin="0,0,5,0">
                                <TextBlock Text="Wheel Diameter (in)" Style="{StaticResource LabelStyle}"/>
                                <ComboBox Style="{StaticResource ComboStyle}" SelectedIndex="1">
                                    <ComboBoxItem Content="2.75"/>
                                    <ComboBoxItem Content="3.25"/>
                                    <ComboBoxItem Content="4.0"/>
                                </ComboBox>
                            </StackPanel>
                            
                            <StackPanel Grid.Column="1" Grid.Row="0" Margin="5,0,0,0">
                                <TextBlock Text="Track Width (in)" Style="{StaticResource LabelStyle}"/>
                                <TextBox Text="12.0" Style="{StaticResource InputStyle}"/>
                            </StackPanel>
                            
                            <StackPanel Grid.Column="0" Grid.Row="1" Margin="0,0,5,0">
                                <TextBlock Text="Gear Ratio" Style="{StaticResource LabelStyle}"/>
                                <TextBox Text="1.0" Style="{StaticResource InputStyle}"/>
                            </StackPanel>
                            
                            <StackPanel Grid.Column="1" Grid.Row="1" Margin="5,0,0,0">
                                <TextBlock Text="Wheel Base (in)" Style="{StaticResource LabelStyle}"/>
                                <TextBox Text="10.0" Style="{StaticResource InputStyle}"/>
                            </StackPanel>
                        </Grid>
                    </StackPanel>
                </Border>
                
                <!-- Tracking Wheels -->
                <Border Background="#2D2D30" Padding="20" CornerRadius="8">
                    <StackPanel>
                        <TextBlock Text="?? Tracking Wheels (Odometry)" Style="{StaticResource HeaderStyle}"/>
                        
                        <CheckBox x:Name="UseTrackingWheels" Content="Use tracking wheels" Foreground="White" IsChecked="True" Margin="0,0,0,10"/>
                        
                        <TextBlock Text="Configuration" Style="{StaticResource LabelStyle}"/>
                        <ComboBox Style="{StaticResource ComboStyle}" SelectedIndex="0">
                            <ComboBoxItem Content="2 Parallel + 1 Perpendicular"/>
                            <ComboBoxItem Content="2 Parallel Only"/>
                            <ComboBoxItem Content="1 Perpendicular Only"/>
                            <ComboBoxItem Content="Motor Encoders Only"/>
                        </ComboBox>
                        
                        <TextBlock Text="Tracking Wheel Diameter (in)" Style="{StaticResource LabelStyle}"/>
                        <TextBox Text="2.75" Style="{StaticResource InputStyle}"/>
                        
                        <TextBlock Text="Horizontal Offset (in)" Style="{StaticResource LabelStyle}"/>
                        <TextBox Text="5.5" Style="{StaticResource InputStyle}"/>
                    </StackPanel>
                </Border>
            </StackPanel>
        </ScrollViewer>
        
        <!-- Center Column - Port Configuration -->
        <ScrollViewer Grid.Column="1" VerticalScrollBarVisibility="Auto" Margin="10,0">
            <StackPanel>
                <Border Background="#2D2D30" Padding="20" CornerRadius="8" Margin="0,0,0,20">
                    <StackPanel>
                        <TextBlock Text="?? V5 Brain Port Configuration" Style="{StaticResource HeaderStyle}"/>
                        
                        <!-- Visual Brain Layout -->
                        <Border Background="#1E1E1E" Padding="20" CornerRadius="5" Margin="0,10,0,20">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>
                                
                                <!-- Left side ports (1-10) -->
                                <StackPanel Grid.Column="0">
                                    <TextBlock Text="Smart Ports 1-10" Foreground="#888888" FontSize="11" Margin="0,0,0,10"/>
                                    <ItemsControl x:Name="LeftPorts">
                                        <ItemsControl.ItemTemplate>
                                            <DataTemplate>
                                                <Border Background="#2D2D30" Margin="2" Padding="8,6" CornerRadius="3">
                                                    <StackPanel Orientation="Horizontal">
                                                        <TextBlock Text="{Binding Port}" Width="20" Foreground="#888888"/>
                                                        <TextBlock Text="{Binding Device}" Foreground="White"/>
                                                    </StackPanel>
                                                </Border>
                                            </DataTemplate>
                                        </ItemsControl.ItemTemplate>
                                    </ItemsControl>
                                </StackPanel>
                                
                                <!-- Right side ports (11-21) -->
                                <StackPanel Grid.Column="1" Margin="10,0,0,0">
                                    <TextBlock Text="Smart Ports 11-21" Foreground="#888888" FontSize="11" Margin="0,0,0,10"/>
                                </StackPanel>
                            </Grid>
                        </Border>
                        
                        <!-- Quick Port Assignment -->
                        <TextBlock Text="Quick Assignment" Style="{StaticResource LabelStyle}" FontWeight="Bold"/>
                        
                        <Grid Margin="0,10,0,0">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="60"/>
                                <ColumnDefinition Width="40"/>
                            </Grid.ColumnDefinitions>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                            </Grid.RowDefinitions>
                            
                            <!-- Headers -->
                            <TextBlock Grid.Row="0" Grid.Column="0" Text="Device" Foreground="#888888" FontSize="11"/>
                            <TextBlock Grid.Row="0" Grid.Column="1" Text="Port" Foreground="#888888" FontSize="11"/>
                            <TextBlock Grid.Row="0" Grid.Column="2" Text="Rev" Foreground="#888888" FontSize="11"/>
                            
                            <!-- Left Front -->
                            <TextBlock Grid.Row="1" Grid.Column="0" Text="Left Front Motor" Foreground="White" VerticalAlignment="Center"/>
                            <TextBox Grid.Row="1" Grid.Column="1" Text="1" Style="{StaticResource InputStyle}" Margin="5,2"/>
                            <CheckBox Grid.Row="1" Grid.Column="2" IsChecked="True" VerticalAlignment="Center"/>
                            
                            <!-- Left Middle -->
                            <TextBlock Grid.Row="2" Grid.Column="0" Text="Left Middle Motor" Foreground="White" VerticalAlignment="Center"/>
                            <TextBox Grid.Row="2" Grid.Column="1" Text="2" Style="{StaticResource InputStyle}" Margin="5,2"/>
                            <CheckBox Grid.Row="2" Grid.Column="2" IsChecked="True" VerticalAlignment="Center"/>
                            
                            <!-- Left Back -->
                            <TextBlock Grid.Row="3" Grid.Column="0" Text="Left Back Motor" Foreground="White" VerticalAlignment="Center"/>
                            <TextBox Grid.Row="3" Grid.Column="1" Text="3" Style="{StaticResource InputStyle}" Margin="5,2"/>
                            <CheckBox Grid.Row="3" Grid.Column="2" IsChecked="True" VerticalAlignment="Center"/>
                            
                            <!-- Right Front -->
                            <TextBlock Grid.Row="4" Grid.Column="0" Text="Right Front Motor" Foreground="White" VerticalAlignment="Center"/>
                            <TextBox Grid.Row="4" Grid.Column="1" Text="4" Style="{StaticResource InputStyle}" Margin="5,2"/>
                            <CheckBox Grid.Row="4" Grid.Column="2" IsChecked="False" VerticalAlignment="Center"/>
                            
                            <!-- Right Middle -->
                            <TextBlock Grid.Row="5" Grid.Column="0" Text="Right Middle Motor" Foreground="White" VerticalAlignment="Center"/>
                            <TextBox Grid.Row="5" Grid.Column="1" Text="5" Style="{StaticResource InputStyle}" Margin="5,2"/>
                            <CheckBox Grid.Row="5" Grid.Column="2" IsChecked="False" VerticalAlignment="Center"/>
                            
                            <!-- Right Back -->
                            <TextBlock Grid.Row="6" Grid.Column="0" Text="Right Back Motor" Foreground="White" VerticalAlignment="Center"/>
                            <TextBox Grid.Row="6" Grid.Column="1" Text="6" Style="{StaticResource InputStyle}" Margin="5,2"/>
                            <CheckBox Grid.Row="6" Grid.Column="2" IsChecked="False" VerticalAlignment="Center"/>
                            
                            <!-- Intake -->
                            <TextBlock Grid.Row="7" Grid.Column="0" Text="Intake Motor" Foreground="White" VerticalAlignment="Center"/>
                            <TextBox Grid.Row="7" Grid.Column="1" Text="7" Style="{StaticResource InputStyle}" Margin="5,2"/>
                            <CheckBox Grid.Row="7" Grid.Column="2" IsChecked="False" VerticalAlignment="Center"/>
                            
                            <!-- IMU -->
                            <TextBlock Grid.Row="8" Grid.Column="0" Text="IMU (Inertial)" Foreground="#00D4FF" VerticalAlignment="Center"/>
                            <TextBox Grid.Row="8" Grid.Column="1" Text="10" Style="{StaticResource InputStyle}" Margin="5,2"/>
                            <TextBlock Grid.Row="8" Grid.Column="2" Text="-" Foreground="#666666" VerticalAlignment="Center" HorizontalAlignment="Center"/>
                            
                            <!-- Rotation Sensors -->
                            <TextBlock Grid.Row="9" Grid.Column="0" Text="Left Rotation Sensor" Foreground="#00FF88" VerticalAlignment="Center"/>
                            <TextBox Grid.Row="9" Grid.Column="1" Text="11" Style="{StaticResource InputStyle}" Margin="5,2"/>
                            <CheckBox Grid.Row="9" Grid.Column="2" IsChecked="True" VerticalAlignment="Center"/>
                            
                            <TextBlock Grid.Row="10" Grid.Column="0" Text="Right Rotation Sensor" Foreground="#00FF88" VerticalAlignment="Center"/>
                            <TextBox Grid.Row="10" Grid.Column="1" Text="12" Style="{StaticResource InputStyle}" Margin="5,2"/>
                            <CheckBox Grid.Row="10" Grid.Column="2" IsChecked="False" VerticalAlignment="Center"/>
                        </Grid>
                    </StackPanel>
                </Border>
                
                <!-- 3-Wire Ports -->
                <Border Background="#2D2D30" Padding="20" CornerRadius="8">
                    <StackPanel>
                        <TextBlock Text="?? 3-Wire (ADI) Ports" Style="{StaticResource HeaderStyle}"/>
                        
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="60"/>
                                <ColumnDefinition Width="100"/>
                            </Grid.ColumnDefinitions>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                            </Grid.RowDefinitions>
                            
                            <TextBlock Grid.Row="0" Grid.Column="0" Text="Mogo Clamp" Foreground="White" VerticalAlignment="Center"/>
                            <TextBox Grid.Row="0" Grid.Column="1" Text="A" Style="{StaticResource InputStyle}" Margin="5,2"/>
                            <ComboBox Grid.Row="0" Grid.Column="2" SelectedIndex="0" Margin="5,2">
                                <ComboBoxItem Content="Pneumatic"/>
                                <ComboBoxItem Content="Digital Out"/>
                            </ComboBox>
                            
                            <TextBlock Grid.Row="1" Grid.Column="0" Text="Doinker" Foreground="White" VerticalAlignment="Center"/>
                            <TextBox Grid.Row="1" Grid.Column="1" Text="B" Style="{StaticResource InputStyle}" Margin="5,2"/>
                            <ComboBox Grid.Row="1" Grid.Column="2" SelectedIndex="0" Margin="5,2">
                                <ComboBoxItem Content="Pneumatic"/>
                                <ComboBoxItem Content="Digital Out"/>
                            </ComboBox>
                            
                            <TextBlock Grid.Row="2" Grid.Column="0" Text="Hang Latch" Foreground="White" VerticalAlignment="Center"/>
                            <TextBox Grid.Row="2" Grid.Column="1" Text="C" Style="{StaticResource InputStyle}" Margin="5,2"/>
                            <ComboBox Grid.Row="2" Grid.Column="2" SelectedIndex="0" Margin="5,2">
                                <ComboBoxItem Content="Pneumatic"/>
                                <ComboBoxItem Content="Digital Out"/>
                            </ComboBox>
                        </Grid>
                        
                        <Button Content="+ Add Device" Style="{StaticResource SecondaryButton}" Margin="0,10,0,0"/>
                    </StackPanel>
                </Border>
            </StackPanel>
        </ScrollViewer>
        
        <!-- Right Column - Code Generation -->
        <ScrollViewer Grid.Column="2" VerticalScrollBarVisibility="Auto">
            <StackPanel Margin="10,0,0,0">
                <Border Background="#2D2D30" Padding="20" CornerRadius="8" Margin="0,0,0,20">
                    <StackPanel>
                        <TextBlock Text="?? Generate Code" Style="{StaticResource HeaderStyle}"/>
                        
                        <TextBlock Text="What to generate:" Style="{StaticResource LabelStyle}"/>
                        <StackPanel Margin="0,5,0,15">
                            <CheckBox Content="Main robot configuration" Foreground="White" IsChecked="True" Margin="0,3"/>
                            <CheckBox Content="Drivetrain control" Foreground="White" IsChecked="True" Margin="0,3"/>
                            <CheckBox Content="Subsystem classes" Foreground="White" IsChecked="True" Margin="0,3"/>
                            <CheckBox Content="Autonomous templates" Foreground="White" IsChecked="True" Margin="0,3"/>
                            <CheckBox Content="PID controller" Foreground="White" IsChecked="True" Margin="0,3"/>
                            <CheckBox Content="LemLib/EZ-Template setup" Foreground="White" IsChecked="True" Margin="0,3"/>
                        </StackPanel>
                        
                        <Button x:Name="GenerateButton" Content="?? Generate PROS Project" Style="{StaticResource PrimaryButton}" FontWeight="Bold"/>
                        
                        <StackPanel Orientation="Horizontal" Margin="0,15,0,0">
                            <Button Content="?? Copy to Clipboard" Style="{StaticResource SecondaryButton}" Margin="0,0,10,0"/>
                            <Button Content="?? Save Files..." Style="{StaticResource SecondaryButton}"/>
                        </StackPanel>
                    </StackPanel>
                </Border>
                
                <!-- Code Preview -->
                <Border Background="#2D2D30" Padding="20" CornerRadius="8">
                    <StackPanel>
                        <StackPanel Orientation="Horizontal" Margin="0,0,0,10">
                            <TextBlock Text="?? Code Preview" Style="{StaticResource HeaderStyle}" Margin="0"/>
                            <ComboBox x:Name="PreviewFileSelect" Margin="20,0,0,0" SelectedIndex="0" Width="150">
                                <ComboBoxItem Content="main.cpp"/>
                                <ComboBoxItem Content="robot.hpp"/>
                                <ComboBoxItem Content="robot.cpp"/>
                                <ComboBoxItem Content="autons.cpp"/>
                            </ComboBox>
                        </StackPanel>
                        
                        <Border Background="#1E1E1E" Padding="15" CornerRadius="5">
                            <TextBox x:Name="CodePreviewBox"
                                     FontFamily="Consolas"
                                     FontSize="11"
                                     Foreground="#9CDCFE"
                                     Background="Transparent"
                                     BorderThickness="0"
                                     IsReadOnly="True"
                                     TextWrapping="NoWrap"
                                     AcceptsReturn="True"
                                     HorizontalScrollBarVisibility="Auto"
                                     VerticalScrollBarVisibility="Auto"
                                     Height="350"
                                     Text="#include &quot;main.h&quot;&#x0a;#include &quot;robot.hpp&quot;&#x0a;#include &quot;autons.hpp&quot;&#x0a;&#x0a;/**&#x0a; * Robot code generated by ControlWorkbench&#x0a; * Team: 1234A&#x0a; * Robot: Competition Bot&#x0a; */&#x0a;&#x0a;void initialize() {&#x0a;    robot::initialize();&#x0a;    chassis.calibrate();&#x0a;    &#x0a;    // Set up auton selector&#x0a;    auton::selector.add_autons({&#x0a;        {&quot;Do Nothing&quot;, auton::do_nothing},&#x0a;        {&quot;Red Positive&quot;, auton::red_positive},&#x0a;        {&quot;Red Negative&quot;, auton::red_negative},&#x0a;        {&quot;Blue Positive&quot;, auton::blue_positive},&#x0a;        {&quot;Skills&quot;, auton::skills}&#x0a;    });&#x0a;}&#x0a;&#x0a;void autonomous() {&#x0a;    auton::selector.run_selected_auton();&#x0a;}&#x0a;&#x0a;void opcontrol() {&#x0a;    while (true) {&#x0a;        robot::drive_tank();&#x0a;        robot::intake_control();&#x0a;        robot::pneumatics_control();&#x0a;        pros::delay(10);&#x0a;    }&#x0a;}"/>
                        </Border>
                    </StackPanel>
                </Border>
            </StackPanel>
        </ScrollViewer>
    </Grid>
</UserControl>
